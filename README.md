# SE331-lab11
Install 
                    
                    1. In ng-app (cmd): npm install
                                        npm install -g @angular/cli
                    
                    2. download: lombok plugin
